{
  "name": "comical-tool-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "npm run build:wasm && tsc --noEmit",
    "build:wasm": "node scripts/build-wasm.mjs",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test:wasm": "npm run build:wasm && node scripts/test-policy-wasm.mjs",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@types/node": "^25.9.3",
    "typescript": "5.7.2",
    "wrangler": "^4.0.0"
  }
}
