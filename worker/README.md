# worker

Cloudflare Worker API adapter for `comical-tool`.

```bash
npm install
npm run cf-typegen
npm run build
npm run dry-run
```

Bindings:

- D1: metadata
- R2: images and files
- KV: clipboard and volatile data
